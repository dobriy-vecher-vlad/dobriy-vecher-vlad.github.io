import { MessageContext } from 'vk-io';
import { IScene } from './scene';
import { StepSceneHandler, IStepContext, IStepSceneOptions } from './step.types';
export declare class StepScene<T = MessageContext> implements IScene {
    slug: string;
    private steps;
    private onEnterHandler;
    private onLeaveHandler;
    constructor(slug: string, rawOptions: IStepSceneOptions<T> | StepSceneHandler<T>[]);
    enterHandler(context: IStepContext & T): Promise<void>;
    leaveHandler(context: IStepContext & T): Promise<unknown>;
}
