import { load as cheerioLoad } from 'cheerio';
import { URL } from 'url';
import { Response } from './fetch-cookie';
export declare type CheerioStatic = ReturnType<typeof cheerioLoad>;
/**
 * Returns the bit mask of the user permission by name
 */
export declare const getUserPermissionsByName: (rawScope: string | string[]) => number;
/**
 * Returns the bit mask of the group permission by name
 */
export declare const getGroupPermissionsByName: (rawScope: string | string[]) => number;
export declare const getAllUserPermissions: () => number;
export declare const getAllGroupPermissions: () => number;
/**
 * Parse form
 */
export declare const parseFormField: ($: unknown) => {
    action: string;
    fields: Record<string, string>;
};
/**
 * Returns full URL use Response
 */
export declare const getFullURL: (action: string, { url }: Response) => URL;
